/**
    DOM: DOCUMENT OBJECT MODEL: MÔ HÌNH TÀI LIỆU DẠNG ĐỐI TƯỢNG
    AI TẠO RA DOM => khi trang web được tải lên thì trình duyệt sẽ tạo ra mô hình cây dom 
    Dựa vào mô hình DOM này chúng ta có thể làm?
        + Thay đổi nội dung trang web
        + Thay đổi các phần tử, thuộc tính,... của các thẻ HTML
        + Tương tác các sự kiện trong phần tử HTML
    CÁC THÀNH PHẦN TRONG DOM
    1. Element (đối tượng)
    Các cách để lấy elimental
    1.lấy theo id : document.getElimentById(); => trả về duy nhất 1 đối tượng 
    2.lấy theo  class: document.getElimentsByClassName ==> trả về HTML colection dữ liệu gần giống mảng
    3.lấy theo tên: document.getElimentsByTagName ===> trả về HTML colection dữ liệu dạng mảng
    2. Atribute (thuộc tính)
    Tương tác với thuộc tính như đối tượng
    
    3. Text (nội dung)
    

 */
function changelog(){
    document.getElementById("heading").style.color
}